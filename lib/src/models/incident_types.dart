import 'package:flutter/material.dart';

class IncidentType {
  Gradient gradient;
  IconData icon;
  String label;

  IncidentType({
    required this.gradient,
    required this.icon,
    required this.label,
  });
}
